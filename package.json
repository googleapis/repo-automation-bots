{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna exec npm test",
    "lint": "lerna exec npm run lint",
    "postinstall": "lerna bootstrap --hoist"
  },
  "devDependencies": {
    "c8": "^5.0.1",
    "lerna": "^3.15.0"
  }
}
