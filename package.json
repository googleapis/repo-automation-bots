{
  "name": "repo-automation-bots",
  "private": true,
  "license": "Apache-2.0",
  "repository": "googleapis/repo-automation-bots",
  "scripts": {
    "test": "c8 lerna exec npm test",
    "lint": "lerna exec npm run lint",
    "fix": "lerna exec npm run fix",
    "postinstall": "lerna bootstrap --hoist",
    "docs-test": "echo 'not yet implemented'",
    "system-test": "echo 'not yet implemented'",
    "proxy": "smee",
    "release": "lerna publish --conventional-commits"
  },
  "devDependencies": {
    "c8": "^5.0.1",
    "lerna": "^3.16.4",
    "smee-client": "^1.1.0"
  },
  "version": "1.0.0"
}
