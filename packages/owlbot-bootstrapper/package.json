{
  "name": "googleapis-bootstrapper",
  "version": "1.0.0",
  "description": "Bootstraps the initial files on new APIs to generate libraries",
  "private": true,
  "author": "Google LLC.",
  "license": "Apache-2.0",
  "repository": "https://github.com/googleapis/repo-automation-bots.git",
  "homepage": "https://github.com/googleapis/repo-automation-bots",
  "bugs": "https://github.com/googleapis/repo-automation-bots/issues",
  "main": "build/src/app.js",
  "keywords": [
    "probot",
    "github",
    "probot-app"
  ],
  "files": [
    "build/src"
  ],
  "scripts": {
    "test": "cd cli && npm i && npm t cd ../common-container && npm i && npm t",
    "fix": "cd cli && npm i && npm run fix && cd ../common-container && npm i && npm run fix",
    "lint": "cd cli && npm i && npm run lint && cd ../common-container && npm i && npm run lint"
  },
  "engines": {
    "node": ">= 18"
  },
  "overrides": {
    "fast-xml-parser": "^5.3.8",
    "ajv": "^8.18.0",
    "eslint": {
      "ajv": "^6.12.4"
    },
    "@eslint/eslintrc": {
      "ajv": "^6.12.4"
    },
    "minimatch@^3": "^3.1.2",
    "minimatch@^5": "^5.1.9",
    "glob": "^10.5.0",
    "tar": "^7.5.8"
  }
}
