{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Conventional Commit Lint Config",
  "description": "Schema for defining the conventional commit lint config",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "enabled": {
      "description": "Should the Conventional Commit Lint bot be enabled.",
      "type": "boolean"
    },
    "always_check_pr_title": {
      "description": "If set to true, the bot will use the PR title",
      "type": "boolean"
    }
  }
}
