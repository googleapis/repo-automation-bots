exports['auto-label responds to events auto detects and labels a Spanner pull request 1'] = {
  'labels': [
    'api: spanner'
  ]
}

exports['auto-label responds to events auto detects and labels a Spanner issue 1'] = {
  'labels': [
    'api: spanner'
  ]
}

exports['auto-label responds to events auto detects and labels a Cloud IoT issue 1'] = {
  'labels': [
    'api: cloudiot'
  ]
}
